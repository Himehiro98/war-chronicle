import { WantaComments } from './types';

/**
 * わんた（マスコット）の各戦争への一言解説
 * tabキー：digest（要点）/ detail（豆知識）/ perspectives（各国視点）/
 *         structure（もしも・構造）/ legacy（後世への影響）
 */
export const WANTA_COMMENTS: Record<string, WantaComments> = {
  // エージェントが順次追加するけん、初期は空っぽで OK
};
